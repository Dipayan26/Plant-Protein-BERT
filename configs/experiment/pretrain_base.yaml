# @package _global_
# Full pretraining run: trembl_full + bert_base
# Usage: python scripts/pretrain.py +experiment=pretrain_base

defaults:
  - override /data: trembl_full
  - override /model: bert_base
  - override /tokenizer: amino_acid
