# requirements.in — direct (unpinned) dependencies
# Compile to requirements.txt with: pip-compile requirements.in
# Or install directly: pip install -r requirements.in

# ── Core runtime ──────────────────────────────────────────────────────────────
torch>=2.2
transformers>=4.40
pytorch-lightning>=2.2
hydra-core>=1.3
omegaconf>=2.3
h5py>=3.10
numpy>=1.26
biopython>=1.83
tokenizers>=0.19
wandb>=0.17
tqdm>=4.66

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
ipykernel>=6.29
jupyter>=1.0
